version: '3.8'

services:
  portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docuware-portal
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
