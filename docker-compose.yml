version: '3.8'

services:
  portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docuware-portal
    ports:
      - "3008:80"
    restart: always
    environment:
      - NODE_ENV=production
